🎯 خلاصة مختصرة (Interview Ready)

English:
Server Components run on the server and send only HTML to the browser.
Since no JavaScript is sent, the browser cannot attach event listeners.
So features like click events, form handlers, animations, or state updates simply do not work. They require client JavaScript, so we must use Client Components.

Arabic:
Server Components بتشتغل على السيرفر وبتبعت HTML بس للمتصفح.
مفيش JavaScript يتحمل، فمفيش أحداث تتربط بالمتصفح.
علشان كده الحاجات اللي محتاجة تفاعل زي click، forms، animations، أو state updates مش هتشتغل.
دول لازم Client Components.

عامية:
السيرفر بيطبخ الأكل ويبعته جاهز.
لو عايز تحرك الأكل أو تضيف ملح → لازم تكون جوه المطبخ (المتصفح).
Server Component = Static
Client Component = تفاعل.